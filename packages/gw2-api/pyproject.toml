[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gw2-api"
version = "1.0.0"
dependencies = ["pydantic", "httpx", "httpx_retries"]

[tool.setuptools.packages.find]
where = ["src"]